<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="WinUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI"
    xmlns:converters="using:WinUI.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Converters -->
            <converters:BoolToVisibleConverter x:Key="BoolToVisible" />
            <converters:BoolToInvisibleConverter x:Key="BoolToInvisible" />
            <converters:BoolInverterConverter x:Key="BoolInverter" />
            <converters:BoolToOpacityConverter x:Key="BoolToOpacity" />
            <converters:BoolToOrangeBrushConverter x:Key="BoolToOrangeBrushConverter" />
            
            <!-- Custom TextBox Style - Keep white background on focus, larger border radius -->
            <Style TargetType="TextBox">
                <Setter Property="BorderBrush" Value="#D8D8D8"/>
                <Setter Property="Background" Value="#FAFAFA"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="12,10"/>
            </Style>

            <!-- Custom PasswordBox Style - Keep white background on focus, larger border radius -->
            <Style TargetType="PasswordBox">
                <Setter Property="BorderBrush" Value="#D8D8D8"/>
                <Setter Property="Background" Value="#FAFAFA"/>
                <Setter Property="CornerRadius" Value="10"/>
                <Setter Property="Padding" Value="12,10"/>
            </Style>
            
            <!-- Custom CheckBox Style - Perfectly circular, Orange when checked -->
            <Style TargetType="CheckBox" x:Key="OrangeCheckBoxStyle">
                <Setter Property="Foreground" Value="#555555"/>
                <Setter Property="CornerRadius" Value="9999"/>
                <Setter Property="Width" Value="20"/>
                <Setter Property="Height" Value="20"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="CheckBox">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CombinedStates">
                                        <VisualState x:Name="UncheckedNormal"/>
                                        <VisualState x:Name="UncheckedPointerOver">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#FF6B35" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#FFF8F5F2" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="UncheckedPressed">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#E55A1F" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#FFF0EBE6" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="UncheckedDisabled"/>
                                        <VisualState x:Name="CheckedNormal">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#FF6B35" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#FF6B35" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPointerOver">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#E55A1F" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#E55A1F" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedPressed">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#CC4D1A" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#CC4D1A" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="CheckedDisabled">
                                            <Storyboard>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBorderBrush" Storyboard.TargetProperty="Color" To="#FFB499" Duration="0"/>
                                                <ColorAnimation Storyboard.TargetName="CheckboxBackgroundBrush" Storyboard.TargetProperty="Color" To="#FFB499" Duration="0"/>
                                            </Storyboard>
                                        </VisualState>
                                        <VisualState x:Name="IndeterminateNormal"/>
                                        <VisualState x:Name="IndeterminatePointerOver"/>
                                        <VisualState x:Name="IndeterminatePressed"/>
                                        <VisualState x:Name="IndeterminateDisabled"/>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Border x:Name="CheckboxBorder"
                                        Grid.Column="0"
                                        BorderThickness="2"
                                        CornerRadius="9999"
                                        Width="20"
                                        Height="20">
                                    <Border.BorderBrush>
                                        <SolidColorBrush x:Name="CheckboxBorderBrush" Color="#D8D8D8"/>
                                    </Border.BorderBrush>
                                    <Border.Background>
                                        <SolidColorBrush x:Name="CheckboxBackgroundBrush" Color="Transparent"/>
                                    </Border.Background>
                                </Border>
                                <ContentPresenter x:Name="ContentPresenter"
                                                 Grid.Column="1"
                                                 Content="{TemplateBinding Content}"
                                                 ContentTemplate="{TemplateBinding ContentTemplate}"
                                                 Margin="8,0,0,0"
                                                 VerticalAlignment="Center"
                                                 TextWrapping="Wrap"/>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Setter Property="Padding" Value="8,8"/>
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="CornerRadius" Value="4"/>
            </Style>
            
            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
