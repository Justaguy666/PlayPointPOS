<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Name="RootCard"
    x:Class="WinUI.Views.UserControls.Dashboard.QuickStatsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI.Views.UserControls.Dashboard"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinUI.Views.UserControls"
    xmlns:vm="using:WinUI.ViewModels.UserControls.Dashboard"
    d:DataContext="{d:DesignInstance Type=vm:QuickStatsControlViewModel, IsDesignTimeCreatable=False}"
    mc:Ignorable="d">

    <Border
        Height="Auto"
        Padding="24"
        Background="{StaticResource WhiteBrush}"
        BorderBrush="{StaticResource LightGrayBrush}"
        BorderThickness="1"
        CornerRadius="12"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Center">
        
        <StackPanel Orientation="Vertical"
                    Spacing="12"
                    VerticalAlignment="Center">

            <StackPanel Orientation="Horizontal"
                Spacing="4"
                VerticalAlignment="Center">
                <PathIcon
                    Width="24"
                    Height="24"
                    Data="{Binding IconState, Converter={StaticResource IconConverter}}"
                    Foreground="{StaticResource PrimaryOrangeBrush}"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center" />

                <TextBlock Text="{Binding Title}"
                           FontWeight="Bold" 
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           Foreground="{StaticResource PrimaryOrangeBrush}" />
            </StackPanel>

            <controls:DividerControl DividerBrush="{StaticResource LighterGrayBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding AverageHoursPerAreaLabel}"
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource DashboardMutedTextBrush}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding AverageHoursPerAreaValue}"
                           FontSize="{StaticResource FontSizeMedium}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource BlackBrush}" />
            </Grid>

            <controls:DividerControl DividerBrush="{StaticResource LighterGrayBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding AverageRevenuePerInvoiceLabel}"
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource DashboardMutedTextBrush}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding AverageRevenuePerInvoiceValue}"
                           FontSize="{StaticResource FontSizeMedium}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource BlackBrush}" />
            </Grid>

            <controls:DividerControl DividerBrush="{StaticResource LighterGrayBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding ReturnRateLabel}"
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource DashboardMutedTextBrush}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding ReturnRateValue}"
                           FontSize="{StaticResource FontSizeMedium}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource BlackBrush}" />
            </Grid>

            <controls:DividerControl DividerBrush="{StaticResource LighterGrayBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding AreaUsageRateLabel}"
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource DashboardMutedTextBrush}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding AreaUsageRateValue}"
                           FontSize="{StaticResource FontSizeMedium}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource BlackBrush}" />
            </Grid>

            <controls:DividerControl DividerBrush="{StaticResource LighterGrayBrush}" />

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0"
                           Text="{Binding NewMembersPerMonthLabel}"
                           FontSize="{StaticResource FontSizeMedium}"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Left"
                           Foreground="{StaticResource DashboardMutedTextBrush}" />

                <TextBlock Grid.Column="1"
                           Text="{Binding NewMembersPerMonthValue}"
                           FontSize="{StaticResource FontSizeMedium}"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           HorizontalAlignment="Right"
                           Foreground="{StaticResource BlackBrush}" />
            </Grid>

        </StackPanel>
    </Border>
</UserControl>
