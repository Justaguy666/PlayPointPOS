<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUI.Views.UserControls.Dashboard.StatCardControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinUI.Views.UserControls"
    mc:Ignorable="d">

    <Border
        Height="Auto"
        Padding="24"
        Background="{StaticResource WhiteBrush}"
        BorderBrush="{StaticResource LightGrayBrush}"
        BorderThickness="1"
        CornerRadius="12"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Center">

        <Grid RowSpacing="8" VerticalAlignment="Center">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <Grid Grid.Row="0" ColumnSpacing="12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border
                    Width="48"
                    Height="48"
                    Background="{StaticResource DashboardStatCardIconBadgeBrush}"
                    CornerRadius="12"
                    VerticalAlignment="Center">
                    <PathIcon
                        Width="24"
                        Height="24"
                        Data="{Binding IconState, Converter={StaticResource IconConverter}}"
                        Foreground="{StaticResource BlackBrush}"
                        HorizontalAlignment="Center"
                        VerticalAlignment="Center" />
                </Border>

                <StackPanel
                    Grid.Column="1"
                    Spacing="2"
                    VerticalAlignment="Center">
                    <TextBlock
                        Text="{Binding Title}"
                        FontSize="{StaticResource FontSizeMedium}"
                        FontWeight="Bold"
                        Foreground="{StaticResource DashboardMutedTextBrush}"
                        TextWrapping="NoWrap"
                        VerticalAlignment="Center" />

                    <TextBlock
                        Text="{Binding ValueText}"
                        FontSize="{StaticResource FontSizeExtraExtraExtraLarge}"
                        FontWeight="Bold"
                        Foreground="{StaticResource PrimaryOrangeBrush}"
                        TextWrapping="NoWrap"
                        TextTrimming="CharacterEllipsis" 
                        VerticalAlignment="Center" />
                </StackPanel>
            </Grid>

            <controls:DividerControl Grid.Row="1" DividerBrush="{StaticResource LighterGrayBrush}" />

            <StackPanel
                Grid.Row="2"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Center">
                <Border
                    Padding="4"
                    Background="{Binding TrendBackground}"
                    CornerRadius="6"
                    VerticalAlignment="Center">
                    <TextBlock
                        Text="{Binding TrendLabel}"
                        FontSize="{StaticResource FontSizeSmall}"
                        FontWeight="SemiBold"
                        Foreground="{Binding TrendForeground}"
                        TextWrapping="NoWrap" 
                        VerticalAlignment="Center" />
                </Border>

                <TextBlock
                    Text="{Binding ComparisonLabel}"
                    FontSize="{StaticResource FontSizeSmall}"
                    FontWeight="SemiBold"
                    Foreground="{StaticResource DashboardMutedTextBrush}"
                    TextWrapping="NoWrap" 
                    VerticalAlignment="Center" />
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
