<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUI.Views.UserControls.HeaderControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinUI.Views.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:WinUI.Views.UserControls" 
    mc:Ignorable="d">

    <Grid Height="80" Padding="0">
        <Grid.Background>
            <StaticResource ResourceKey="HeaderGradientBrush" />
        </Grid.Background>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Today's Revenue -->
        <StackPanel Grid.Column="0" Margin="32,0,48,0" Padding="0,16" VerticalAlignment="Center">
            <TextBlock Text="{Binding TodayRevenueLabelDisplay}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeMedium}" />
            <TextBlock Text="{Binding TodayRevenue}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeExtraExtraExtraLarge}" FontWeight="Bold" Margin="0,2,0,0" />
        </StackPanel>

        <!-- Active Tables -->
        <StackPanel Grid.Column="1" Margin="0,0,48,0" Padding="0,16" VerticalAlignment="Center">
            <TextBlock Text="{Binding ActiveTablesLabelDisplay}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeMedium}" />
            <TextBlock Text="{Binding ActiveTables}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeExtraExtraExtraLarge}" FontWeight="Bold" Margin="0,2,0,0" />
        </StackPanel>

        <!-- Reserved Tables -->
        <StackPanel Grid.Column="2" Padding="0,16" VerticalAlignment="Center">
            <TextBlock Text="{Binding ReservedTablesLabelDisplay}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeMedium}" />
            <TextBlock Text="{Binding ReservedTables}" Foreground="{StaticResource WhiteBrush}" FontSize="{StaticResource FontSizeExtraExtraExtraLarge}" FontWeight="Bold" Margin="0,2,0,0" />
        </StackPanel>

        <!-- Logo -->
        <controls:LogoControl Grid.Column="4" 
                              Width="170" Height="60" 
                              Margin="0,0,20,0" 
                              HorizontalAlignment="Right" 
                              VerticalAlignment="Center" />
    </Grid>
</UserControl>
